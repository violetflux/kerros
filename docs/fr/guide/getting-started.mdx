import { PackageManagerTabs } from '@rspress/core/theme'
import CounterDemo from '../../_components/CounterDemo'

# Bien démarrer

## Installation

<PackageManagerTabs command={{
  npm: 'npm install @violetflux/kerros',
  pnpm: 'pnpm add @violetflux/kerros',
  yarn: 'yarn add @violetflux/kerros',
  bun: 'bun add @violetflux/kerros',
}} />

Kerros est un paquet npm standard compatible avec npm, pnpm, Yarn et Bun. Le gestionnaire utilisé par le dépôt Kerros ne limite pas votre application. React 17, 18 et 19 sont pris en charge.

## Créer un store

`createStore` reçoit un Hook React et renvoie le Hook consommateur ainsi que le Provider correspondant.

```tsx
import { createStore } from '@violetflux/kerros'
import { useState } from 'react'

export const [useCounter, CounterProvider] = createStore(() => {
  const [count, setCount] = useState(0)
  return { count, setCount }
})
```

Montez le Provider puis sélectionnez les champs nécessaires :

```tsx
function Counter() {
  const { count, setCount } = useCounter(s => ({
    count: s.count,
    setCount: s.setCount,
  }))

  return <button onClick={() => setCount(count + 1)}>{count}</button>
}
```

Les sélecteurs peuvent être déclarés en ligne sans `useCallback`.

<CounterDemo />

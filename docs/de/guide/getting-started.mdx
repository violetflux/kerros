import { PackageManagerTabs } from '@rspress/core/theme'
import CounterDemo from '../../_components/CounterDemo'

# Erste Schritte

## Installation

<PackageManagerTabs command={{
  npm: 'npm install @violetflux/kerros',
  pnpm: 'pnpm add @violetflux/kerros',
  yarn: 'yarn add @violetflux/kerros',
  bun: 'bun add @violetflux/kerros',
}} />

Kerros ist ein normales npm-Paket und funktioniert mit npm, pnpm, Yarn und Bun. Der Paketmanager des Kerros-Repositories schränkt deine Anwendung nicht ein. React 17, 18 und 19 werden unterstützt.

## Store erstellen

`createStore` erhält einen React Hook und gibt den Consumer Hook sowie den passenden Provider zurück.

```tsx
import { createStore } from '@violetflux/kerros'
import { useState } from 'react'

export const [useCounter, CounterProvider] = createStore(() => {
  const [count, setCount] = useState(0)
  return { count, setCount }
})
```

Provider einbinden und benötigte Felder auswählen:

```tsx
function Counter() {
  const { count, setCount } = useCounter(s => ({
    count: s.count,
    setCount: s.setCount,
  }))

  return <button onClick={() => setCount(count + 1)}>{count}</button>
}
```

Selektoren dürfen inline stehen und benötigen kein `useCallback`.

<CounterDemo />

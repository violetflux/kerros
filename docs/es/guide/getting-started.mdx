import { PackageManagerTabs } from '@rspress/core/theme'
import CounterDemo from '../../_components/CounterDemo'

# Primeros pasos

## Instalación

<PackageManagerTabs command={{
  npm: 'npm install @violetflux/kerros',
  pnpm: 'pnpm add @violetflux/kerros',
  yarn: 'yarn add @violetflux/kerros',
  bun: 'bun add @violetflux/kerros',
}} />

Kerros es un paquete npm estándar compatible con npm, pnpm, Yarn y Bun. El gestor usado por el repositorio de Kerros no limita tu aplicación. Es compatible con React 17, 18 y 19.

## Crear un store

`createStore` recibe un Hook de React y devuelve el Hook consumidor y su Provider correspondiente.

```tsx
import { createStore } from '@violetflux/kerros'
import { useState } from 'react'

export const [useCounter, CounterProvider] = createStore(() => {
  const [count, setCount] = useState(0)
  return { count, setCount }
})
```

Monta el Provider y selecciona los campos necesarios:

```tsx
function Counter() {
  const { count, setCount } = useCounter(s => ({
    count: s.count,
    setCount: s.setCount,
  }))

  return <button onClick={() => setCount(count + 1)}>{count}</button>
}
```

Los selectores pueden escribirse en línea y no necesitan `useCallback`.

<CounterDemo />

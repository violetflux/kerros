name: Publish package

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - run: bun install --frozen-lockfile
      - run: bun run lint
      - run: bun run typecheck
      - run: bun run test
      - run: bun run build
      - run: npm install --global npm@12.0.1
      - name: Check Kerros package
        run: npm pack --dry-run
      - name: Check ESLint plugin package
        run: npm pack --dry-run --workspace @violetflux/eslint-plugin-kerros
      - name: Check published Kerros version
        id: runtime-version
        shell: bash
        run: |
          PACKAGE=$(node -p "require('./package.json').name")
          VERSION=$(node -p "require('./package.json').version")
          if npm view "$PACKAGE@$VERSION" version >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Publish Kerros to npm
        if: steps.runtime-version.outputs.published != 'true'
        run: npm publish --access public --provenance
      - name: Check published ESLint plugin version
        id: plugin-version
        shell: bash
        run: |
          PACKAGE=$(node -p "require('./packages/eslint-plugin-kerros/package.json').name")
          VERSION=$(node -p "require('./packages/eslint-plugin-kerros/package.json').version")
          if npm view "$PACKAGE@$VERSION" version >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Publish ESLint plugin to npm
        if: steps.plugin-version.outputs.published != 'true'
        run: npm publish --access public --provenance --workspace @violetflux/eslint-plugin-kerros
